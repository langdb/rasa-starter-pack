version: "3.1"

slots:
  return_value:
    type: any
    mappings:
      - type: custom
        action: add_contact
      - type: custom
        action: remove_contact

responses:
  utter_greet:
    - text: "Hello, how may I help you?"